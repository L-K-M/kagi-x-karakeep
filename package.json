{
  "name": "kagi-x-karakeep",
  "private": true,
  "description": "Shows Karakeep bookmark results beside Kagi search results.",
  "scripts": {
    "lint": "web-ext lint",
    "build": "web-ext build",
    "sign": "web-ext sign --channel=unlisted",
    "start": "web-ext run"
  },
  "devDependencies": {
    "web-ext": "^10.4.0"
  }
}
